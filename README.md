# 凱撒衛浴選品試算工具 MVP v24

## 本版修正
1. 額外勾選真正收合。
   - 預設全部隱藏。
   - 該品項數量大於 0 才顯示。
   - 數量回到 0 時會自動隱藏，並取消已勾選狀態。

2. 快取處理。
   - `index.html` 加入 no-cache meta。
   - CSS / JS 改用版本檔名：
     - `style-v202607300957.css`
     - `app-v202607300957.js`
   - 並加上 `?v=202607300957`。
   - 每次新版上傳後，不再容易讀到舊 CSS / JS。

3. 保留 v23 功能。
   - 預設折數 45。
   - 所有數量預設 0。
   - 浴櫃 / 鏡櫃寬度用公分輸入，內部轉 mm 比對。

## 上傳注意
請把 zip 裡所有檔案都上傳到 GitHub 根目錄，不要只上傳 index.html。
